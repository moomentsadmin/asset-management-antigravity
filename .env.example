# ==============================================
# 🚀 ASSET MANAGEMENT - PRODUCTION CONFIGURATION
# ==============================================
# Rename this file to .env before starting
# Fill in the values below

# --- 1. CORE SETTINGS ---
NODE_ENV=production
PORT=5000
AUTO_INIT_DB=true

# --- 2. DOMAIN & SSL (CRITICAL) ---
# Your domain name (e.g., asset.digile.com)
DOMAIN_NAME=asset.yourdomain.com

# --- 3. DATABASE CONFIGURATION ---
# Use the service name 'mongodb' for Docker
MONGODB_URI=mongodb://admin:password@mongodb:27017/asset-management?authSource=admin
MONGO_USER=admin
MONGO_PASSWORD=password

# --- 4. SECURITY SECRETS (CHANGE THESE!) ---
# Generate a random 32-char string: openssl rand -base64 32
JWT_SECRET=change_this_to_a_long_random_secret_string_in_production
JWT_EXPIRE=7d

# --- 5. INITIAL ADMIN USER ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@123456
# This email is used for Certbot notifications AND the initial app admin
ADMIN_EMAIL=admin@yourdomain.com

# --- 6. APP CUSTOMIZATION ---
COMPANY_NAME=Your Company Name
CURRENCY=USD
TIMEZONE=America/New_York

# --- 7. API EXPOSURE ---
# URL where the frontend can reach the backend (Public URL)
FRONTEND_URL=https://${DOMAIN_NAME}
API_URL=https://${DOMAIN_NAME}/api

# --- 8. EMAIL (OPTIONAL) ---
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
